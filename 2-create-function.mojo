fn main() -> None:
    var x: Int = 0 ## Mutable
    x += 1
    print(x)
    let y: Int = 10 # Immmutable
    print(y)
    # y += 1 # error 



    # var x: Int = 1 # var to create mutable (can change)
    # let y: Int = 10 # let to create immutable (can't change)
    # x+= 1
    # print(x)